Criptografia assimetrica


ssh-keygen -t rsa -b 4096 -c "rosefelix99@gmail.com"
    -keygen
    Gerador de chaves

    -t rsa
    Indica qual algoritmos vamos utilizar

    -b 4096
    Quatidade de seguranção, 4096 é um numero padrao de bits

    -c "rosefelix99@gmail.com"
    Informar qual email utilizado na conta GitHub

cd ~/.ssh
Para verificar se os arquivos foram criados na pasta (id_rsa e od_rsa.pub)

cat id_rsa.pub
Mostrar chave publica

eval "$(ssh-agent -s)"
Iniciar Agente de SHH (funciona como servidor e o terminal retorna o pid(numero de prosso do agente ssh))

ssh-add ~/.ssh/id_rsa
Adicionar a chave privada ao agente

